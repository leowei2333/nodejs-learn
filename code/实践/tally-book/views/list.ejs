<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/list.css">
  <title>账单列表</title>
</head>
<body>
  <h1>账单列表</h1>
  <hr>
  <div class="list">
    <% accounts.forEach(item => { %>
      <div class="item <%= item.type === '1' ? 'isin' : 'isout' %>">
        <p>事项：<%= item.title %></p>
        <p>时间：<%= item.time %></p>
        <p>类型：<%= item.type === '1' ? '收入' : '支出' %></p>
        <p>金额：<%= item.account %></p>
        <p>备注：<%= item.remark %></p>
      </div>
    <% }) %>
  </div>
</body>
</html>